source = ~/.config/hypr/colors.conf

general {
    hide_cursor = true
    ignore_empty_input = true
}

animations {
    enabled = true
    bezier = ease, 0.25, 0.1, 0.25, 1.0
    animation = fadeIn, 1, 3, ease
    animation = fadeOut, 1, 3, ease
    animation = inputFieldDots, 1, 2, ease
}

background {
    monitor =
    path = screenshot
    blur_passes = 3
    blur_size = 8
    brightness = 0.6
}

# Time
label {
    monitor =
    text = $TIME
    color = $theme_fg
    font_size = 96
    font_family = $theme_font
    position = 0, 200
    halign = center
    valign = center
}

# Date
label {
    monitor =
    text = cmd[update:60000] date +"%A, %B %d"
    color = $theme_fg
    font_size = 20
    font_family = $theme_font
    position = 0, 110
    halign = center
    valign = center
}

# Input field
input-field {
    monitor =
    size = 300, 50
    outline_thickness = 2
    dots_size = 0.25
    dots_spacing = 0.2
    dots_center = true
    outer_color = $theme_accent
    inner_color = $theme_bg1
    font_color = $theme_fg
    check_color = $theme_green
    fail_color = $theme_red
    fade_on_empty = false
    font_family = $theme_font
    placeholder_text = <i>Password</i>
    fail_text = <i>$FAIL</i>
    rounding = 8
    position = 0, -20
    halign = center
    valign = center
}

# "Hi, kevin"
label {
    monitor =
    text = Hi, $USER
    color = $theme_fg
    font_size = 14
    font_family = $theme_font
    position = 0, -70
    halign = center
    valign = center
}
