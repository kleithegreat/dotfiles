# Monitor Configuration — Laptop
# Adjust per-host: copy to hosts/desktop/monitors.conf for desktop overrides

# Built-in display
monitor = eDP-1, 1920x1200, 0x0, 1

# External displays (auto-detect position)
monitor = , preferred, auto, 1
