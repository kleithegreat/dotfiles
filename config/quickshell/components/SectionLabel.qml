import QtQuick
import ".." as Root

// Uppercase section heading used inside settings and quick panes.
Text {
    color: Root.Theme.fg4
    font.family: Root.Theme.fontFamily
    font.pixelSize: Root.Theme.fontSizeSmall
    font.bold: true
}
