diff -ruN -x '*.orig' -x '*.rej' a/main.cpp b/main.cpp
--- a/main.cpp
+++ b/main.cpp
@@ -5,5 +5,6 @@
 #include <hyprland/src/Compositor.hpp>
+#include <hyprland/src/output/Monitor.hpp>
 #include <hyprland/src/desktop/state/FocusState.hpp>
 #include <hyprland/src/desktop/view/Window.hpp>
 #include <hyprland/src/config/ConfigManager.hpp>
 #include <hyprland/src/config/shared/actions/ConfigActions.hpp>
@@ -22,0 +24,2 @@
+#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
+
@@ -56 +60 @@ static void hkAddDamageA(void* thisptr, const CBox& box) {
-    const auto PMONITOR = (CMonitor*)thisptr;
+    const auto PMONITOR = (Monitor::CMonitor*)thisptr;
@@ -67 +71 @@ static void hkAddDamageB(void* thisptr, const pixman_region32_t* rg) {
-    const auto PMONITOR = (CMonitor*)thisptr;
+    const auto PMONITOR = (Monitor::CMonitor*)thisptr;
@@ -296 +300 @@ APICALL EXPORT PLUGIN_DESCRIPTION_INFO PLUGIN_INIT(HANDLE handle) {
-    FNS = HyprlandAPI::findFunctionsByName(PHANDLE, "_ZN8CMonitor9addDamageERKN9Hyprutils4Math4CBoxE");
+    FNS = HyprlandAPI::findFunctionsByName(PHANDLE, "addDamageERKN9Hyprutils4Math4CBoxE");
@@ -298,2 +302,2 @@ APICALL EXPORT PLUGIN_DESCRIPTION_INFO PLUGIN_INIT(HANDLE handle) {
-        failNotif("no fns for hook _ZN8CMonitor9addDamageERKN9Hyprutils4Math4CBoxE");
-        throw std::runtime_error("[he] No fns for hook _ZN8CMonitor9addDamageERKN9Hyprutils4Math4CBoxE");
+        failNotif("no fns for hook addDamageERKN9Hyprutils4Math4CBoxE");
+        throw std::runtime_error("[he] No fns for hook addDamageERKN9Hyprutils4Math4CBoxE");
diff -ruN -x '*.orig' -x '*.rej' a/ExpoGesture.cpp b/ExpoGesture.cpp
--- a/ExpoGesture.cpp
+++ b/ExpoGesture.cpp
@@ -7 +7 @@
-#include <hyprland/src/helpers/Monitor.hpp>
+#include <hyprland/src/output/Monitor.hpp>
diff -ruN -x '*.orig' -x '*.rej' a/overview.cpp b/overview.cpp
--- a/overview.cpp
+++ b/overview.cpp
@@ -1,6 +1,7 @@
 #include "overview.hpp"
 #include <algorithm>
 #include <any>
+#include <chrono>
 #include <cmath>
 #define private   public
 #define protected public
@@ -241,21 +241,26 @@
     Cursor::overrideController->setOverride("left_ptr", Cursor::CURSOR_OVERRIDE_UNKNOWN);
 
     lastMousePosLocal = g_pInputManager->getMouseCoordsInternal() - pMonitor->m_position;
+    const auto OPENED_AT = Time::steadyNow();
 
     auto onCursorMove = [this](Event::SCallbackInfo& info) {
         if (closing)
             return;
 
         info.cancelled    = true;
         lastMousePosLocal = g_pInputManager->getMouseCoordsInternal() - pMonitor->m_position;
     };
 
-    auto onCursorSelect = [this](Event::SCallbackInfo& info) {
+    auto onCursorSelect = [this, OPENED_AT](Event::SCallbackInfo& info) {
         if (closing)
             return;
 
         info.cancelled = true;
 
+        // Ignore accidental touchpad/button events that arrive as the overview opens.
+        if (std::chrono::duration_cast<std::chrono::milliseconds>(Time::steadyNow() - OPENED_AT).count() < 200)
+            return;
+
         selectHoveredWorkspace();
 
         close();
@@ -282,7 +289,7 @@
     if (!pMonitor)
         return;
 
-    if (pMonitor->m_activeWorkspace != startedOn && !closing) {
+    if (valid(startedOn) && pMonitor->activeWorkspaceID() != startedOn->m_id && !closing) {
         // likely user changed.
         onWorkspaceChange();
     }
@@ -422,15 +427,10 @@
 
         const auto NEWIDWS = g_pCompositor->getWorkspaceByID(NEWID);
 
-        const auto OLDWS = pMonitor->m_activeWorkspace;
-
         if (!NEWIDWS)
             Config::Actions::changeWorkspace(std::to_string(NEWID));
         else
             Config::Actions::changeWorkspace(NEWIDWS->getConfigName());
 
-        g_pDesktopAnimationManager->startAnimation(pMonitor->m_activeWorkspace, CDesktopAnimationManager::ANIMATION_TYPE_IN, true, true);
-        g_pDesktopAnimationManager->startAnimation(OLDWS, CDesktopAnimationManager::ANIMATION_TYPE_OUT, false, true);
-
         startedOn = pMonitor->m_activeWorkspace;
     }
 
@@ -471,7 +471,7 @@
 void COverview::fullRender() {
     const auto GAPSIZE = (closing ? (1.0 - size->getPercent()) : size->getPercent()) * GAP_WIDTH;
 
-    if (pMonitor->m_activeWorkspace != startedOn && !closing) {
+    if (valid(startedOn) && pMonitor->activeWorkspaceID() != startedOn->m_id && !closing) {
         // likely user changed.
         onWorkspaceChange();
     }
